{% if image %}
            <img src="{{ thumbnail(image, 1368, 1026) }}" alt="{{ image.alt|default() }}">
        {% if image.alt|default() %}
            <figcaption>{{ image.alt }}</figcaption>
        {% endif %}
{% endif %}