{% if background_image %}
<!-- background image -->
    <figure>
            <img class="background-image" src="{{ thumbnail(record, 1368, 1026) }}" alt="{{ (record|image).alt|default(record|title) }}">
        {% if image.alt|default() %}
            <figcaption>{{ image.alt }}</figcaption>
        {% endif %}
    </figure>
<!-- background image -->
{% endif %}